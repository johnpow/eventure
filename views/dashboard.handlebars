<div>
  <h1 class="text-center container underline">My Future Events</h1>
{{#each activities as |activity| }}
{{#if activity.isFutureEvent}}
  <div class="container my-3 py-1">
      <div class="row justify-content-center">
        <div class="col-lg-8 bucksCard banner">
          <div class="d-flex align-items-center">
            <img class="activityIcon" src="/images/{{activity.activity_category}}.png" width="60" height="60" alt="{{activity.activity_category}} Icon"/>
            <h3 class="d-flex textPadding text-turquoise justify-content-start"><a class="plainText" href="/updateEvent/{{activity.id}}">{{activity.activity_title}}</a></h3>
          </div>
          <div class="bg-light p-4 blackText">
            <p>{{activity.activity_description}}</p>
            <p><b>Date and Time: </b>{{activity.activity_date}}</p>
            <p><b>Location: </b>{{activity.activity_location}}</p>
            <p><b>Category: </b>{{activity.activity_category}}</p>
            <div class="d-flex justify-content-between align-items-center">
              <a id ="manageEvent" href="/updateEvent/{{activity.id}}">Manage Event</a>
              <button
                  class="btn btn-outline-dark btn-sm "
                  id="view-comment-btn"
                  type="button"
                  data-bs-toggle="collapse"
                  data-bs-target="#view-comment-list{{activity.id}}"
                  aria-expanded="false"
                  aria-controls="view-comment-list"
                >View Attendees  ({{activity.signupsCount}})</button>
            </div>
            <div class="collapse mt-3" id="view-comment-list{{activity.id}}">
                <ol> 
                  <div>
                    <b><li>@{{activity.user.username}} (You)</li></b>
                  </div>                  
                {{#each activity.signups as |signup|}}
                  <div>
                    <b><li>@{{signup.user.username}}</li></b>
                  </div>
                {{/each}}
                </ol>
              </div>
          </div>
              
        </div>
     </div>

  </div>
{{/if}}
{{/each}}
</div>

<div>
  <h1 class="text-center container underline">My Past Events</h2>
{{#each activities as |activity| }}
{{#unless activity.isFutureEvent}}
  <div class="container my-3 py-1">
      <div class="row justify-content-center">
        <div class="col-lg-8 bucksCard banner">
          <div class="d-flex align-items-center">
            <img class="activityIcon" src="/images/{{activity.activity_category}}.png" width="60" height="60" alt="{{activity.activity_category}} Icon"/>
            <h3 class="d-flex textPadding text-turquoise justify-content-start"><a class="plainText" href="/updateEvent/{{activity.id}}">{{activity.activity_title}}</a></h3>
          </div>
          <div class="bg-light p-4 blackText">
            <p>{{activity.activity_description}}</p>
            <p><b>Date and Time: </b>{{activity.activity_date}}</p>
            <p><b>Location: </b>{{activity.activity_location}}</p>
            <p><b>Category: </b>{{activity.activity_category}}</p>
            <div class="d-flex justify-content-between align-items-center">
              <a id ="manageEvent" href="/updateEvent/{{activity.id}}">Manage Event</a>
              <button
                  class="btn btn-outline-dark btn-sm "
                  id="view-comment-btn"
                  type="button"
                  data-bs-toggle="collapse"
                  data-bs-target="#view-comment-list{{activity.id}}"
                  aria-expanded="false"
                  aria-controls="view-comment-list"
                >View Attendees  ({{activity.signupsCount}})</button>
            </div>
            <div class="collapse mt-3" id="view-comment-list{{activity.id}}">
                <ol> 
                  <div>
                    <b><li>@{{activity.user.username}} (You)</li></b>
                  </div>                  
                {{#each activity.signups as |signup|}}
                  <div>
                    <b><li>@{{signup.user.username}}</li></b>
                  </div>
                {{/each}}
                </ol>
              </div>
          </div>
              
        </div>
     </div>

  </div>
{{/unless}}
{{/each}}
</div>


